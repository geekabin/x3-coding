

CALL XABC(X1,X2,X3)		#CALL调用XABC方法

#子程序XABC

SUBGROG XABC(W1,W2,W3) 	#子程序	

#什么参数的类型

VALUE CHAR W1	     #当调用call方法时传入的x1参数 在当前代码块内发生改变时，外部不x1不发生变化

​	W1 = '我是值传递'

​	W1 = '我被改掉了'    #CALL里的参数X1不会变

VARIABLE INIEGER W2  #当调用call方法时传入的x2参数 在当前代码块内发生改变时，外部X2会随着子程序的改变而改变  

​										 #传进来为100，修改为200之后，CALL XABC(X1,X2,X3)之后的代码里的X2=200

​	W2 = 100

​	W2 = 200  				

VALUE DECIMAL W3			#内部w3被改变，外部x3的值还是不变 为15.6

​		W3 = 15.6

​		W3 = 17.22

#当



END

